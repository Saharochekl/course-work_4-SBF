# Память проекта SBF

Актуально на 2026-09-10. Это структурированная передача контекста между сессиями
Codex. Она не заменяет численные CSV/FITS и не является сырым экспортом чата.

## Как начать новую сессию

### Исключение локальных документов из выпуска — 2026-09-10

- Исходный HEAD `90ee97e` (`ready for cleaning stage_2`), worktree был чистым.
  Пользователь сообщил об удалении очередного trash и коммите предыдущих правок.
- По явному запросу `git rm --cached` снял с отслеживания ровно восемь файлов:
  пять PDF корневого `materials/`, два подписанных PDF из `texts/course_work/`
  и `texts/report_work/`, чужой `AndreevMI/AndreevMI.tex`.
- Все восемь сохранены на прежних локальных путях побайтно без изменений.
  В `.gitignore` добавлены точечные правила: root-only `/materials/*.pdf`,
  точные пути двух signed PDF и весь чужой каталог AndreevMI.
- `.gitignore` и исключения подготовлены в индексе, новый коммит не создавался.
  Код, notebook, научные данные, собственные статьи/рисунки и curated CSV не менялись.
- История Git не переписывалась: исключённые документы всё ещё доступны в старых
  коммитах. Это не полное удаление из истории и не гарантия их приватности.
- Технические пункты (ненужная запись FITS, gc/reflog/history) пользователь
  прямо запретил трогать в этом ходе; они не выполнялись.

### Повторная очистка после удаления trash (новее разделов ниже)

- Исходный HEAD `6662585` (`trash deleted`). Пользователь удалил весь прежний
  `trash/2026-09-09/`, включая legacy и журналы прошлых переносов. Не считать
  их существующими резервными копиями. Git не вернёт удалённые ignored-данные.
- В `trash/2026-09-09-followup/removed/` физически перенесены ещё 140
  непотребляемых диагностических FITS (9.050497 GiB), 299 временных QA/cache/
  Finder-файлов и один `.fits.interrupted`; всего 440 файлов, 9.095050 GiB.
  Три TSV в новом карантине содержат полные исходные пути, размеры и причины.
  Никакого окончательного удаления или filesystem aliases.
- FITS-группы: full_frame, model_support_mask, premask_initial_compact_mask,
  premask_before_mcut, extra_candidates_compactresidmask — по 28 файлов.
  Потребителей сохранённых файлов нет в актуальных py/3notebook/builders/JSON.
  Массивы в памяти продолжают использоваться; source-notebook всё ещё записывает
  эти экспорты при новом прогоне. Удаление ненужной записи — возможная будущая
  правка, НЕ выполненная в текущем аудите.
- Сохранены full_science для before-catalog contact sheet, актуальные нормированные
  монтажи/кольца, model_full, catalog/external masks, raw SCI, PSF/OPD, выбранные
  input/E(k), журналы, CSV/TeX, все outputs и научные исходники.
- После переноса: layout35figures/864paths, F150source14/14,
  F150normalized14/14, F090published14/14; AST36Python/138cells;
  13целевыхunit-тестов passed. Научные ячейки/прогоны не запускались.
- Дополнительный read-only reuse-аудит после переноса подтвердил F150 source
  completion14/14, F090 final_result_valid14/14, source/config keys и выбранные
  compact-input/E(k)14/14 в каждом фильтре. Повторная обработка не требуется.
- Семь README/docs/ignore-комментариев исправлены: старый trash удалён;
  tracked-история и ignored-данные — разные вещи. Научный код и статья не менялись.
- В Git нет FITS/NPZ/кэшей/логов/SQLite/локальных MD, кроме двух README.
  Но signed отчёты и чужой AndreevMI-образец всё ещё tracked; предложено владельцу
  исключить из публичного релиза, пока НЕ удаляли/не untrack.
- `.git` около 1.2 GiB; gc/reflog/history rewrite не запускались. Дальнейшее
  обслуживание Git требует отдельного решения, не путать с удалением trash.

### Физический карантин 2026-09-09 (актуальнее разделов ниже)

- Исходный HEAD `35a0141`. Пользователь явно потребовал перенести мусор самими
  файлами в trash; никаких filesystem aliases, даже оставленных в архиве.
- Перенесены 292 файла из девяти списков `trash/2026-09-09/*.txt` (49.062GiB)
  и весь `code/legacy/` (249 обычных файлов, 3.197GiB).
- Новый адрес каждого: `trash/2026-09-09/removed/<старый относительный путь>`.
  Карта: `moved_files.txt`. Копий/ссылок на старых местах нет; данные не уничтожены.
- Удалены только 244 старых symlink-объекта, их реальные цели не тронуты;
  исторические адреса записаны обычным текстом в `removed_links.txt`.
- В code/data/runs/texts/materials/trash/.cache нет символических ссылок.
  Системные ссылки Python-окружения и Git не трогать: они не научные артефакты.
- Не создавать symlink/hardlink или Finder aliases для реорганизации проекта.
  Переносить реальные файлы и исправлять реальные пути, не делать заменителей.
- Код обработки и научные ячейки не менялись; текущие модели/маски/остатки/PSF,
  актуальные 14+14 кэши, журналы и таблицы для рисунков остаются рабочими.
- После переноса подтверждены source-ready F150W14/14 и final_valid F090W14/14;
  input/E(k)/config/source keys14/14 для каждого фильтра. Проверка layout:
  35 рисунков,864 пути; в этом проходе запущены13 целевых тестов, все прошли.
  Научных запусков не было.
- `sbf.check_project_layout` теперь проверяет отсутствие симлинков, включая trash;
  правило не создавать filesystem aliases записано в AGENTS.md.
- Перенос внутри диска не освобождает место: окончательное удаление trash пользователь
  делает лично. Сириловские .seq не переносились.

### Структура после ревью 2026-09-09 (новее всех путей ниже)

- Исходный HEAD этой правки `22d3e36`, ветка review. Научные вопросы пользователь
  отложил; в этой сессии только переносы, интерфейсы запуска и проверки.
- В корне `code/` ровно `download.py`, `process.py` и три рабочих notebook:
  `sbf-2.ipynb` (общий измерительный шаблон), `sbf-2-graph.ipynb`,
  `sbf-f090w-graph.ipynb`. Пакеты: `sbf/`, `figures/`; тесты: `tests/`;
  манифесты: `config/`; три литературных CSV: `reference/`.
- Экспериментальные systematics/normalized-winsor notebooks, recovery/summarize,
  их специализированные тесты и старый sbf2_batch_outputs — в
  `code/legacy/review-2026-09-09/`, не в Git. Production не импортирует legacy.
- `runs/` содержит только `F150W/` и `F090W/`. Карта старых путей:
  `sbf2_go3055/analysis` → `F150W/analysis`, остаток `sbf2_go3055` → `F150W/source`,
  `sbf2_normalized_winsor` → `F150W/spectra`, `sbf_f090w_go3055` → `F090W`.
  Служебные кэши вынесены в `.cache/`. На начало работы пользователь уже удалил
  старые `runs/legacy` и `runs/sbf2_systematics`; не считать их существующими.
- Активные filesystem aliases отсутствуют; 193 run-ссылки перенесены в trash,
  products.json и связанные пути заменены физическими путями. Старые code/data,
  code/materials/code/runs/code/analysis ссылки также в локальном карантине.
- Метаданные JSON/CSV/SQLite перебазированы с сохранением mtime; резервные копии
  в `trash/2026-09-09/metadata_before`. Исторические fingerprint-описания сохранены;
  core канонизирует только путь для cache identity, не использует старые пути для I/O.
- Проверены F150W source-ready 14/14 и оба спектральных результата/кэша 14/14.
  Перенос не требует повторного моделирования или FFT. Проверка источников статьи:
  35/35; live metadata paths: 864. Научные ячейки не выполнялись, outputs сохранены.
- Финальный полный набор: 111/111 тестов; AST 36 Python и 138 ячеек трёх notebook.
  `process.py --filter both --check` прошёл: F150 source14/14, spectra14/14,
  F090 complete14/14. F150 source dry-run read-only; F090 штатная проверка
  может обновить служебные логи/готовность/шаблон, но не выполняет измерения.
- Первый реальный F150 запуск может завести новый служебный campaign (старые
  завершены, SHA шаблона другой), но проверенный done-ledger 14/14 принимается
  до workerloop; изофоты не пересчитываются. Не путать campaignrun с новым фитами.
- Все текущие команды из `code/`: `py download.py images --program 3055 --download`,
  `py download.py opd --program 3055 --download`, `py process.py --filter F150W`,
  `py process.py --filter F090W`; проверки `py -m sbf.check_project_layout --with-products`,
  `py -m unittest discover -s tests -t .`. Расширенный CLI: `py -m sbf.<module>`.
- Корневой `Readme.md` полностью английский; `Readme_RUS.md` русский; только эти
  два Markdown в Git. Память, AGENTS, REVIEW_FINDINGS и прочие заметки игнорируются.
- Список удаления: локальный `CLEANUP_CANDIDATES.md` и точные TXT в
  `trash/2026-09-09/`: 224 промежуточных FITS ≈45.248GiB, условно старые F150W
  2.263GiB, старые F090W caches0.479GiB, четыре F277W/F356W1.073GiB. FITS не удалялись.
  Сохранить сырой F090W/F150W, модели, маски, текущие остатки, PSF, таблицы и worker.log.
- Курсовые рисунки перенесены в `texts/course_work/materials/figures`, используемая
  TeX-таблица — в `materials/tables`. В TeX исправлены только пути, научный текст нет.

### Чистка кода и Git от 2026-09-08 (новее предыдущего ревью)

- Ветка `review`, исходный HEAD `929c978`; изменения пока не закоммичены.
- По прямому указанию пользователя все личные Markdown, включая эту память и
  `AGENTS.md`, исключены из Git, но сохранены локально. Не добавлять их обратно.
  Исключение — публичный корневой `Readme.md`; документация кода RU/EN — `docs/*.rst`.
- Удалены только из индекса 804 уже игнорируемых пути: заметки, старые архивы,
  временные продукты и сборки. Git-история не переписывалась. Физически данные
  не удалены; правила `.gitignore` не означают разрешение удалить текущие кэши.
- Актуальные команды и ограничения: `Readme.md`, `docs/measurement.rst`,
  `docs/infrastructure.rst`, `docs/analysis.rst`, `docs/repository.rst`,
  `docs/notebook_parameters.rst`. Старые локальные README могут быть неактуальны.
- У F150W runner удалён дублирующий downloader/cleanup: загрузка только через
  `download_go3055_go7763.py`; входные FITS обработчик не скачивает и не удаляет.
  SBF3-служебные реализации/тесты и курсовой regression baseline сохранены в
  `code/legacy/review-2026-09-08/`, не входят в рабочий набор тестов.
- `psutil` и Astropy теперь обязательны; самодельные запасные парсеры памяти,
  metadata-only FITS-проверка и неиспользуемые SQLite-обёртки удалены.
  Реальные сетевые повторы, восстановление файлов и F090W изофотный QC оставлены.
- Из `sbf-2.ipynb` удалены 6 неиспользуемых функций и 50 неиспользуемых параметров;
  оставшиеся численные настройки сохранены. Исходные ячейки, outputs и счётчики
  notebook не пересчитывались. F090W graph notebook синхронизирован с генератором.
- Сборщики безопасно импортируются; убраны непубликуемые дублирующие рисунки.
  Проверка сборщика таблиц в отдельном каталоге: 21 научный CSV/TeX совпал
  побайтно; provenance закономерно меняется при изменении исходного кода.
- Проверка текущих продуктов: 14/14 F150W source_key совпадают, 14/14 F090W
  final_result_valid=ok; 867 ссылок существуют. Статическая проверка: 31 Python,
  5 notebook / 174 code cells, 35 публикационных рисунков. Научных прогонов нет.
- Общий набор после чистки: 100 unit-тестов проходят. У всех пяти notebook сохранены
  outputs/execution_count. 22 публикационных PNG при изолированном построении
  совпали побайтно. 49 функций FFT-ядра и 35 оставшихся функций исходного notebook
  имеют прежнее численное AST (в ядре значения вынесены в именованные константы).
- Полностью дочитаны исходники обоих больших F150W notebook (graph/systematics).
  Исследовательские notebook оставлены как проверки метода; не считать,
  что каждый их исторический блок полностью переписан или больше не нужен.
  Полный независимый запуск с нуля и побитовая воспроизводимость не проверены.
- В systematics исправлена проверка происхождения PSF 257: cache hit не меняет
  FITS/версии генератора. Три существующих кэша совместимы. В normalized notebook
  картинки явно выводятся PNG независимо от Agg. В F150 graph исправлены
  подписи, повторная запись/накопление строк, silent NaN и bootstrap guards.
- Научные вопросы старых диагностик — в локальном `REVIEW_FINDINGS.md`:
  два cluster-anchor бюджета, имя И смысл аннулярной ковариации, сокращённые
  ошибки нелинейных моделей, неравномерные histogram bins и смысл physical_solution.
  Они не исправлены под видом рефакторинга; первичные индивидуальные расстояния
  не пересчитывались. Не обещать полную готовность научного релиза до этого разбора.
- Публичные изменения подготовлены в индексе, коммит/переписывание истории не сделаны.
- Кандидаты для ручного удаления с ограничениями и размерами — `docs/repository.rst`.
  Текущие данные, worker.log (источник ошибки фона), PSF/OPD и таблицы не удалять.

### Ревью структуры от 2026-09-08

- Ветка `review`, основа перед изменениями `2bb3299`; нового научного прогона нет.
- Актуальная карта кода: `code/README.md`; переносы/проверки: `REVIEW_CHANGES.md`.
  Предыдущий `REPOSITORY_AUDIT.md` описывает состояние ДО этих переносов.
- F090W runner/support/manifest и загрузчики отслеживаются Git. Исключено дерево
  результатов F090W, а не его код; старый список «ещё не закоммичены» ниже исторический.
- SBF-3, monitor и старые analysis/catalog_search перенесены в
  `code/legacy/review-2026-09-08/`. Старые данные — в `runs/legacy/review-2026-09-08/`,
  включая 25 неактуальных поколений F090W-спектров. Все файлы сохранены;
  старые адреса данных поддерживают относительные symlink. Не удалять их вслепую.
- Все 35 подключаемых/закомментированных изображений английской и русской статьи
  находятся в `texts/paper_work/materials/figures/`. Manifest источников —
  `materials/figure_sources.json`; `publish_article_assets.py` обновляет копии.
  TeX-научный текст и численные таблицы не менялись, английский PDF пересобран в build/.
- `sbf_paths.py` разрешает пути от расположения проекта и читает исторические
  абсолютные адреса из текущего checkout, без перезаписи JSON/CSV/provenance.
- Синтаксис пяти notebook проверен без выполнения; outputs сохранены.
  `py check_project_layout.py --with-products` проверяет код, картинки и 867 путей
  принятых продуктов без измерения SBF. F150W source_key сохранился для 14/14.
- F090W final_result_valid=ok для14/14, source_key/config=14/14, input fingerprints=28/28.
  Повторный source/FFT-прогон из-за ревью не нужен; force-флаги не добавлять.
- Два LegacyGo3055AdoptionTests исторически падают из-за отсутствующих старых FITS;
  это не проблема текущих 14/14 F150W/F090W. Подробности в REVIEW_CHANGES.md.

### Редакция статьи от 2026-09-05: приложения и аудит

- Основа этой редакции: Git HEAD `706c9e6dc4e21a7fcd7aba2353620b5fcc32551e`;
  правки текста и рисунков сделаны поверх него, без нового измерительного прогона.
- Статья уже включает оба фильтра: F150W constant и F090W linear, все 14 галактик.
  Старые пункты ниже «после F090W 14/14» — исторический план, не актуальный статус статьи.
- По прямому указанию пользователя удалены из TeX прежние Figures 21, 23, 26, 27
  и Table 11. Данные и notebook-проверки не удалять и не возвращать рисунки автоматически.
- Сохранены Figures 18–20 как изображения. В Figure 18 добавлена сноска: нормальность
  общей пиксельной гистограммы не обязательна; хвосты/асимметрия требуют пространственной
  и спектральной диагностики. Сама гистограмма показывает поле ДО винзорирования.
- Прежняя Figure 22 теперь Figure 21; прежняя Figure 28 теперь Figure 24;
  прежняя Table 13 теперь Table 12. Из таблицы ПО удалены только SHA256-строки;
  версии и Git-идентификаторы сохранены, полные SHA остаются в CSV.
- В Figures 2, 3, 10 восстановлены реальные принятые горизонтальные 1σ:
  `sigma_color_total` (F150W), `sigma_color_adopted_mag` (F090W), примерно
  0.0031–0.0054 mag. Графики строит `code/build_go3055_article_figures.py`.
  Серая полоса — ±σ_int, НЕ доверительный интервал средней линии.
- Погрешности в статье выражать как σ в mag/Mpc, не процентами. RMS — отдельная
  эмпирическая метрика, не переименовывать автоматически в σ. Доли пикселей и PSF-потока
  допустимо указывать в процентах: это не погрешности.
- Исправлены фактические формулировки: синтетическое дополнительное смещение около
  0.001 mag мало, но статистически разрешено; нормировка НЕ отменяет космологическое
  ослабление потока; маски/модели разных фильтров строятся отдельно. Обычная K-поправка
  не задаёт доказанную верхнюю границу SBF K-поправки; внутренняя пыль оценена условно,
  не измерена. Ковариация в формулах явно относится к общему foreground reddening;
  остальные фотометрические ковариации и эффективное число независимых пикселей не закрыты.
- Длинный перечень файлов/команд вынесен в `texts/paper_work/README_REPRODUCIBILITY.md`.
  Все вспомогательные продукты LaTeX и визуального контроля — в `texts/paper_work/build/`.
- Численные таблицы калибровок/расстояний и научные notebook не изменялись и не запускались.

### Порядок входа в проект

1. Прочитать `AGENTS.md` и этот файл целиком.
2. Проверить `git status --short` и не затирать незакоммиченную работу.
3. Проверить `runs/sbf_f090w_go3055/target_status.csv`: статус F090W может быть новее
   этого документа.
4. Для F090W прочитать `code/SBF_F090W_RUN.md`.
5. По статье F150W сверяться с `texts/paper_work/go3055_jwst_sbf_article_draft.tex` и
   `runs/sbf2_go3055/analysis/tables/`, а не со старыми графиками из курсовой.

## Рабочие привычки пользователя

- Пользователь запускает команды из `code/`.
- `astro_env` обычно уже активировано; команда Python для пользователя — `py`.
- Не добавлять `MPLBACKEND=Agg` в команды для пользователя. Codex обязан использовать
  `Agg` в своих служебных запусках Matplotlib: иначе macOS аварийно завершает Python
  в `FigureCanvas_new -> NSApplication -> _RegisterApplication -> abort`.
- По умолчанию не запускать notebook-ячейки: их запускает пользователь после restart kernel.
- Код notebook должен быть простым и читаемым: меньше одноразовых функций и ненужных
  `assert`, больше последовательных ячеек с Markdown-объяснениями.
- Фигуры в notebook должны и сохраняться, и отображаться inline; не закрывать фигуру до вывода.
- Долговечные продукты только в рабочей директории. `/private/tmp` — только для одноразовых
  служебных файлов, не для результатов.
- Не качать массово архивы JWST/HST без явного согласия: место на диске ограничено.
- Предпочтительный язык общения — русский; английский — для текста научной статьи и её фигур.

## Научная цель

Проект воспроизводит и проверяет методологию surface brightness fluctuations (SBF) на
JWST/NIRCam и сверяет полученные расстояния с TRGB. Текущая статья по F150W ограничена
14 галактиками GO-3055. Основной ориентир — Jensen et al. Paper III
(`materials/2502.15935v2.pdf`); якорные индивидуальные TRGB-расстояния взяты из Paper IV
(`materials/2603.11160v1.pdf`).

Цель не в том, чтобы любой ценой найти цветовую зависимость. Главный вопрос — даёт ли
реализация SBF точные расстояния на шкале TRGB. Цвет — потенциальная поправка на звёздное
население, а не часть определения SBF.

## Выборка GO-3055

NGC 1380, 1399, 1404, 1549, 3379, 4374, 4406, 4472, 4486, 4552, 4621, 4636, 4649, 4697.

Среда:

- Fornax: NGC 1380, 1399, 1404;
- строгая Virgo-подвыборка в статье: семь объектов; не путать с девятью объектами в широкой
  области Virgo;
- NGC 1549 и NGC 3379 — Other;
- NGC 4636 и foreground NGC 4697 не входят в строгие семь Virgo.

## Замороженный F150W-анализ

Ветка `paper_work`, коммит перед началом F090W: `c3506ae` (`new graphs + corrections of Figures
2, 5, 7, 11. Ready to make F090W SBF`). Основа замороженного F150W-анализа — коммиты `d89e094` и
`ad5785a`.

Основные файлы:

- `code/sbf-2.ipynb` — замороженный измерительный notebook F150W;
- `code/run_sbf_2_batch.py` — batch GO-3055 для Jensen-like F150W;
- `code/run_sbf_2_normalized_winsor.py` и `code/sbf2_normalized_winsor_core.py` — финальная ветвь
  нормировки/винзорирования;
- `code/sbf-2-graph.ipynb` — сводная статистика, таблицы и фигуры статьи;
- `code/sbf-2-systematics.ipynb` — тесты шума, PSF и винзорирования;
- `code/sbf-2-normalized-winsor.ipynb` — эксперимент с обрезкой после нормировки;
- `runs/sbf2_go3055/analysis/` — актуальные CSV, TeX-таблицы и фигуры.

Принятая измерительная схема:

- signal: F150W;
- population color: F090W-F150W, измерен в тех же двух кольцах;
- круговые кольца 8.2–16.4 и 16.4–32.8 arcsec для сопоставимости с Jensen;
- модель галактики — интерполяция яркости и геометрии между измеренными эллиптическими
  изофотами; Sersic нужен для заливки щелей/дыр при фите, а не как финальная модель;
- остаток `(SCI - sky - model) / sqrt(model)`;
- после нормировки выполняется симметричное винзорирование 3.5 sigma по всей
  валидной области с положительной моделью, а не отдельно по кольцам;
- это именно winsorization: выходы за порог заменяются границей, а не удаляются;
- маскированные/невалидные пиксели не входят в FFT;
- из каждого кольца перед FFT вычитается среднее, чтобы убрать DC/нулевую частоту; это
  не удаляет SBF-дисперсию;
- основной фит: `P(k) = P0 E(k) + P1`;
- модель с отдельным `N(k)` была проверена и отклонена: она ухудшала фит/давала неустойчивую
  дегенерацию;
- `kmin = 0.01, 0.03, 0.04`, `kmax = 0.25`; публикуемая ветвь берёт `kmin=0.04`;
- `E(k)` строится из PSF и реального window/mask кольца;
- PSF: STPSF 129x129, ближайший WSS OPD плюс четыре смещения по полю детектора;
- размер 129 сверен с 257; эффект невелик и выбор 129 обоснован сопоставимостью;
- `P0 = P_SBF + P_r`; `P_r` — остаточная мощность неразрешённых шаровых скоплений и фоновых галактик;
- для этой выборки `P_r/P0 < 0.67%`, поправка меньше примерно 0.007 mag; тесты `P_r=0`, adopted,
  `2P_r` и отдельных ширин GCLF не меняют вывод;
- GCLF = globular cluster luminosity function, функция светимости шаровых скоплений.

## Статистический вывод F150W

Основная калибровка заранее зафиксирована как:

- все 14 галактик;
- индивидуальные Paper IV thin-slice TRGB-модули;
- без ручного исключения объектов;
- постоянный цветонезависимый нуль-пункт как основная модель;
- линейная и нелинейные цветовые законы — только диагностика/чувствительность.

Текущие числа из аннотации статьи:

- `Mbar_150,0 = -3.208 +/- 0.024_stat +/- 0.051_common mag`;
- fitted intrinsic scatter = 0.081 mag;
- bootstrap 16–84% interval для intrinsic scatter: 0.064–0.088 mag;
- LOO-смещение модуля = -0.0010 mag;
- LOO RMS = 0.0925 mag = 4.3% по расстоянию;
- медианная внутренняя предсказанная ошибка = 0.0865 mag = 4.1%;
- с общей ошибкой абсолютной TRGB-шкалы = 0.0984 mag = 4.6%;
- RMS нормированных LOO-остатков = 1.08;
- 8/14 в пределах 1 sigma, 14/14 в пределах 2 sigma;
- линейная цветовая модель хуже: LOO RMS 0.0981 mag, Delta AICc = 3.15, bootstrap-интервал
  наклона включает ноль.

Формулировка вывода должна быть холодной: данные F150W пока не требуют цветовой зависимости;
постоянный нуль-пункт статистически конкурентоспособен и принят. Не писать просто «метод
работает» без чисел разброса и валидации.

LOO = leave-one-out: каждая галактика по очереди исключается из калибровки, а затем её расстояние
предсказывается по остальным 13. Это внутренняя кросс-валидация, не независимая внешняя
проверка.

## Научные ограничения и нельзя-выводы

- Не объявлять Virgo–Fornax step обнаружением: цвет и скопление смешаны, Fornax всего 3,
  exact permutation p около 0.23, модель со ступенькой проигрывает по AICc.
- Не объявлять NGC 1399 выбросом в основном опыте. Она отклоняется примерно на 2.2 sigma;
  красивая парабола без неё — постфактум-тест чувствительности.
- Основные расстояния: индивидуальные Paper IV thin-slice. Thick-slice — обязательная проверка
  чувствительности. Средние по Virgo/Fornax — буквальное сравнение с Paper III, не основа.
- Сравнение Jensen 2015 F160W есть только для 5 общих галактик. Не смешивать его с Paper III F110W.
- Внутренние кольца визуально/статистически стабильнее внешних, но оба оставлены для
  повторения геометрии Jensen. Одни и те же угловые кольца охватывают разные физические радиусы.
- Показатель `M150_model_aperture` — не полная светимость, а светимость модели внутри 32.8 arcsec.
  График связи SBF с ней — исследовательский; общая корреляция p=0.20, после цветового вычитания
  p около 0.056; обнаружением не считать.
- Внутренняя пыль, K-поправка и космологическое потускнение оценены как малые для этих
  близких E/S0; количественные границы есть в статье.

## Ошибки F150W

Индивидуальная ошибка измерения apparent SBF складывается в квадратуре из:

- разброса спектрального фита/окон `P(k)`;
- разброса по PSF-реализациям;
- фона;
- неопределённости `P_r`;
- погасания в Млечном Пути.

Для точки абсолютной калибровки добавляется индивидуальная ошибка TRGB-модуля. Для предсказания
расстояния также нужны intrinsic scatter калибровки и конечная точность нуль-пункта. Общая
ошибка абсолютной TRGB-шкалы не уменьшается от усреднения галактик и должна показываться отдельно.

Измерительные столбики на графике могут быть маленькими, но реальный LOO-разброс больше из-за
intrinsic scatter и калибровочной неопределённости. Не смешивать «точность измерения `mbar`» с
«точностью предсказанного расстояния».

## Визуальные оценки остатков F150W

Эти оценки даны пользователем вручную; не переписывать их автоматическими метриками:

| Galaxy | Score | User assessment |
|---|---:|---|
| NGC 3379 | 10.0 | Эталон; исключительно ровный белый шум |
| NGC 1549 | 9.0 | Очень слабая крупномасштабная структура |
| NGC 4621 | 7.0 | Крестообразная крупномасштабная структура |
| NGC 4406 | 8.0 | Слабая крестообразная структура |
| NGC 4374 | 8.0 | Аналогичная слабая структура |
| NGC 4486 | 3.0 | Сильный градиент и плохо вырезанный джет; идеально замаскировать джет не получится |
| NGC 4697 | 6.0 | Сильная структура и неоднородности |
| NGC 4649 | 5.0 | Сильный градиент |
| NGC 4636 | 6.0 | Структура и градиент |
| NGC 4552 | 9.0 | Почти идеал; немного градиента |
| NGC 4472 | 4.0 | Очень сильный градиент |
| NGC 1404 | 8.0 | Немного структуры |
| NGC 1399 | 9.5 | Внешняя звезда вырезана аккуратно |
| NGC 1380 | 7.0 | Градиент и структура |

Недостаточно большая область сама по себе не считается проблемой; градиенты и крупномасштабная
структура считаются.

## Статья F150W

- Английский двухколоночный черновик: `texts/paper_work/go3055_jwst_sbf_article_draft.tex`.
- Русская версия: `texts/paper_work/go3055_jwst_sbf_article_draft_ru.tex`.
- Таблицы измерений подключаются из `go3055_jwst_sbf_article_measurements.tex`.
- В финальном тексте Result/основные результаты должны быть ближе к концу статьи, после метода
  и систематик.
- Блок о цвете должен отдельно показывать, что зависимость исследована, но для F150W не принята.
- Все публикационные графики — на английском; маскированные области на монтаже остатков — чёрные.
- Подача рисунков пересмотрена 2026-09-05: одна ровная серая полоса вокруг принятой
  калибровки показывает `±sigma_int`, как в референсе пользователя. Это не доверительный
  интервал линии и не полная ошибка расстояния. В сравнении одинаковых восьми галактик
  и в диагностике апертурной величины полоса показывает наблюдаемый остаточный разброс
  `±s_res`, с явным определением в подписях. Ошибки коэффициентов остаются в таблицах/тексте.
- На графиках галактик использовать Fornax / Virgo region / Other и подписывать все NGC;
  не заменять среду классификацией чистоты. Основные фиты по-прежнему используют 14/14.
- F150W-линейная калибровка вновь показана отдельным рисунком рядом с константной.
  Она остаётся диагностикой: улучшения LOO нет, интервал наклона включает ноль.
- Рисунки сопоставления цвета/SBF по отдельным кольцам временно закомментированы в TeX;
  файлы рисунков сохранены. Для возврата достаточно раскомментировать блоки.
- `|Delta mbar|_ann` в таблицах измерений — полная разность колец, не полуразность.
  Исправлены подписи; сами значения и научные оценки ошибок не изменялись.

Не выводить в публикуемую таблицу два столбца «SBF_const» и «SBF_linear» как будто это два равноправных
набора расстояний. Публикуемый результат — constant calibration; linear нужен в таблице сравнения моделей.

## Курсовая и отдельные тексты

- Курсовая — историческая версия работы. В текущей F150W-статье её не упоминать.
- Келдышевские чтения: `texts/Келдышевские-чтения/`; туда входит только то, что было в курсовой/сделано
  до 2026-06-10. Текст на русском, оформлен по выданному образцу; не менять заголовок `main.tex` и не добавлять
  стилевые пакеты.
- Техническое объяснение `P_r`: `texts/paper_work/materials/pr_unresolved_sources_explanation.tex`.

## F090W: текущая кампания

Это отдельная офлайн-кампания; она не меняет замороженный F150W-анализ.

Файлы, которые на 2026-09-04 ещё не закоммичены:

- `code/run_sbf_f090w.py`;
- `code/sbf090_pipeline_support.py`;
- `code/targets_go3055_f090w_manifest.csv`;
- `code/test_run_sbf_f090w.py`;
- `code/SBF_F090W_RUN.md`.

Параметры:

- signal: F090W;
- F150W — локальный вспомогательный вход; в текущем source-stage он не загружается в память;
- те же кольца 8.2–16.4 и 16.4–32.8 arcsec;
- центр переносится из F150W через WCS и локально уточняется;
- старты изофот 40, 50, 60, 70, 100 px; каждая полная ветвь обязана дойти до внешнего кольца и
  пройти единый QC;
- крупный внешний загрязнитель маскируется, если он ярче `S/N=100` и целиком дальше 250 px от центра;
- нормализация/винзорирование и спектральная схема аналогичны принятой F150W-ветви;
- F090W PSF: STPSF 129x129, ближайший WSS OPD плюс 4 положения;
- человекочитаемый restart-state: `runs/sbf_f090w_go3055/target_status.csv`;
- итоговая таблица: `runs/sbf_f090w_go3055/f090w_sbf_results.csv`.

Состояние на момент записи этого файла:

- все 14/14 галактик завершены; первые 13 визуально приняты пользователем, NGC 4636 после
  исправления ещё требует окончательного визуального просмотра;
- NGC 4636 была единственной проблемой;
- причина NGC 4636: F090W-детектор компактных источников принял структуру самой галактики за много
  компактных источников и создал почти замкнутое кольцо маски на 120–200 px;
- кольцо лежит внутри начала SBF-области (около 263 px), но не даёт Ellipse дойти до неё;
- старая схема давала 79 изофот с замороженной геометрией (`stop_code=4`) в научных кольцах и не должна
  приниматься;
- исправление точечное: только при построении изофот NGC 4636 компактная premask игнорируется внутри
  8.2 arcsec/около 263 px;
- NaN/щели детекторов продолжают маскироваться;
- итоговая science-mask и SBF-измерение не изменены;
- 19/19 статических/unit-тестов прошли;
- точечный повтор NGC 4636 завершился 2026-09-04 03:29:01 MSK со статусом `ok/complete`;
- итоговые продукты найдены и прошли машинную проверку; это закрывает технический сбой, но не заменяет
  визуальную проверку модели, изофот и нормированных остатков.

Команда для нового принудительного повтора, если он действительно понадобится, из `code/`:

```bash
py run_sbf_f090w.py --galaxies "NGC 4636" --force-source --force-spectra
```

## Что делать после F090W 14/14

1. Визуально проверить NGC 4636: модель, изофоты, полный нормированный остаток, оба FFT-кольца.
2. Убедиться, что в `f090w_sbf_results.csv` 14 строк и для всех closure/QC проходят.
3. Закоммитить F090W runner, support, manifest, tests, documentation, `AGENTS.md`, `PROJECT_MEMORY.md`,
   `.cbmignore` и изменение `.gitignore`. Не коммитить `runs/` и `.codebase-memory/`.
4. Построить F090W абсолютную калибровку по индивидуальным Paper IV TRGB-модулям.
5. Проверить зависимость `Mbar_090` от локального цвета F090W-F150W. Для F090W физически ожидается
   более сильная популяционная зависимость, чем в F150W; её наличие не предрешать.
6. Сравнить constant и linear по AICc, bootstrap slope, LOO RMS и predictive intervals; не выбирать модель по красоте графика.
7. Построить коридоры неопределённости средней зависимости и отдельные predictive corridors.
8. Проверить F090W PSF 129 против 257 на нескольких характерных объектах до финальной публикации.
9. Добавить в приложение гистограммы пикселей до/после нормировки, с границами 3/3.5/4 sigma, для каждой
   галактики/фильтра.

## Не смешивать с текущей статьёй

- `code/sbf-3.ipynb`, `code/sbf-3_debug.ipynb`, `code/run_sbf_batch.py` — обобщённая многофильтровая/многопрограммная
  ветвь. Она нужна для GO-7763 и дальнейшей масштабной работы, но не для текущей F150W-статьи GO-3055.
- GO-7763 J-Virgo, GO-5989 Coma, PEARLS/COSMOS-Web/CEERS, Cen A, M104 и GO-8277 — будущее расширение, не текущая калибровка.
- По GO-8277 данные будут открыты примерно в середине 2027 года; в программе есть M105 и NGC 4278.

## История ключевых решений

- До 2026-06-10: курсовая/исходная реализация.
- 2026-07: создан `sbf-3` для других пар фильтров; подготовлены загрузчики/манифесты GO-3055 и GO-7763;
  затем для текущей статьи возврат к `sbf-2`, чтобы не уходить от Jensen.
- В методе были исправлены фон, primary mask, full-frame изофотная модель, старты изофот, PSF, `P_r`, диапазон `k`
  и сохранение промежуточных FITS.
- Эксперимент с `kmax` до 0.5 и альтернативными `E(k)` признан неудачным; production вернут к `window_only`,
  `kmax=0.25`.
- Отрицательный `P1` в простом фите сам по себе не физичен, но служит диагностикой несовершенства фона/модели;
  расширенная `P0E + PnN` модель не дала более надёжного результата.
- Нормировка на `sqrt(model)` размазывает сырые хвосты распределения, поэтому винзорирование после
  нормировки было проверено и выбрано. Улучшение есть, но оно не огромное.
- Синтетический injection/recovery или recovery-test нужен для проверки перевода единиц/нормировки: в известные
  модель, PSF и маску вставляется известный SBF-сигнал, после чего весь FFT-контур должен восстановить
  его. Такая проверка есть в систематическом notebook/таблицах; она не заменяет проверку на независимых галактиках.

## Как обновлять эту память

После каждого существенного решения или нового полного прогона Codex должен обновить:

- дату в начале файла;
- актуальный Git commit;
- статус F090W/F150W;
- принятые параметры и причины;
- текущий TODO;
- ссылки на новые итоговые таблицы и фигуры.
